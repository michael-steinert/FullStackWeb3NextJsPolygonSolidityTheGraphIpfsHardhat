{
  "name": "blog",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ michael-steinert/blog",
    "create-local": "graph create --node http://localhost:8020/ michael-steinert/blog",
    "remove-local": "graph remove --node http://localhost:8020/ michael-steinert/blog",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 michael-steinert/blog"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
